apiVersion: v1
kind: PersistentVolume
metadata:
  annotations:
    pv.kubernetes.io/provisioned-by: blob.csi.azure.com
  name: pv-blob-data
spec:
  capacity:
    storage: 200Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: blob-fuse-mi
  mountOptions:
    - -o allow_other
    - --block-cache
    - --tmp-path=/mnt/blobcache
  csi:
    driver: blob.csi.azure.com
    # make sure volumeHandle is unique for every storage blob container in the cluster
    volumeHandle: "nearora-rg#kuberaysa#xgboost"
    volumeAttributes:
      resourceGroup: nearora-rg
      storageAccount: kuberaysa
      containerName: xgboost
      protocol: fuse
      AzureStorageAuthType: MSI
      AzureStorageIdentityClientID: "8f229821-3235-49ca-8213-989f75b93d39"